# Python deps for the preview-stl skill.
trimesh>=4.0      # STL load/bake/export for the viewer; proximity queries for the sim
numpy>=1.24
scipy>=1.10       # trimesh.proximity backend
rtree>=1.0        # fast closest-point queries (the sim is unusable without it)
#
# Headless page verification (optional but recommended):
#   npm i -g playwright && npx playwright install chromium
