# Printer credentials / local config — NEVER commit (contains LAN access code)
# Covers per-printer variants (bambu.x2d.toml, bambu.p1s.toml, …) but not
# bambu.toml.example, which is the committed placeholder template.
bambu.toml
bambu.*.toml
!bambu.toml.example

# Local printer quickstart with real IP/serial filled in — not for the public repo
docs/x2d-guide.typ

# Slicer build output + generated viewers + design previews
*.gcode
*.gcode.3mf
# Bambu Studio CLI drops this in cwd on every slice
result.json
*.viewer.html
*.iso.png
*.front.png
*.top.png
*.right.png

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# macOS
.DS_Store
local/
